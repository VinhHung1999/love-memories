// T472 (Sprint 70) — Memory Map. Thin Expo Router route file. View lives in
// `src/screens/Map/MapScreen.tsx`; this is just the file-based-routing entry.
import { MapScreen } from '@/screens/Map/MapScreen';

export default function MapRoute() {
  return <MapScreen />;
}
