// Sprint 67 T460 — Editorial scroll for monthly recap. Reachable from
// ActionsTray "Xem chi tiết" CTA inside Stories. ?month=YYYY-MM passed
// through; ViewModel falls back to previous full month when absent.

import { MonthlyRecapScreen } from '@/screens/Recap';

export default MonthlyRecapScreen;
