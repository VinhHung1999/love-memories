// Sprint 67 T458 — Top-level route for RecapArchive (push transition, not
// modal). Same pattern as notifications + daily-questions. Reachable
// from Profile → "Lưu trữ recap" row.

import { RecapArchiveScreen } from '@/screens/RecapArchive';

export default RecapArchiveScreen;
